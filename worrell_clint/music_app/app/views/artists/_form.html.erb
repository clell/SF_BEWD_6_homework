<div class="well">
<%= simple_form_for @artist do |f| %>
  <%= f.input :name, label: false, input_html: {:class => 'form-control', placeholder: "Artist Name"} %><br/>
  <%= f.submit "Submit".html_safe, :class => 'btn btn-lg btn-success' %>
<% end %>
</div>